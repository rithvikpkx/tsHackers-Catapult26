# ML Service Placeholder

The v1 Grind build keeps normalization, decomposition, profile updates, risk scoring, and intervention logic inside the Next.js application for faster iteration.

This directory remains as a future experimentation surface if a separate Python model service becomes necessary later.
